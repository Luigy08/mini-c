Program areas;

Var x, x1, x2: integer; 
Var y1 : char;
Var x3: boolean;
Var x4: boolean;
Var testString: string;

type esRecord  = record
		atributo1: char;
        atributo2: integer;
end;

Var ejemploRecord : esRecord;

Function N(test: integer; z: char; prueba: char): integer;

Var t, o, x: integer;
Var testRecord : esRecord;
Var ejemp: char;


Begin 
    x := test + testRecord.atributo2;

    if  x < x2 AND t > 3 OR o > 10 then 
        x:= 6 + 5; 
End;

Begin
         x := N(x, y1, y1);
         x := 1 * 2;
         write('testString', x1);
End.